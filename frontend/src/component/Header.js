import { Fragment } from "react";
import Nav from "./Nav";

const Header = () => {
  return (
    <Fragment>
      <header>
        <Nav />
      </header>
    </Fragment>
  );
};

export default Header;
